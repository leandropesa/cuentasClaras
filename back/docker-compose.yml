services:
  postgres:
    image: postgres:16
    container_name: cuentasclaras-db
    environment:
      POSTGRES_DB: cuentasclaras
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: